/** @type {import('@arpadroid/module').BuildConfigType} */
const config = {
    deps: ['resources'],
    buildTypes: true,
    storybook_port: 6016,
    buildType: 'uiComponent'
};
export default config;
